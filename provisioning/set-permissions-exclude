# Files and directories to exclude from set-permissions.sh.

# These are regular expressions that are applied to the output of the 'find'
# command used in all permission changers in set-permissions.sh.

# Blank lines and lines starting with # are ignored.

^\.*$
